{
  "name": "top-gun-geo-lens",
  "version": "1.0.0",
  "description": "TOP GUN GEO-Lens: Brand visibility auditing across LLMs. Quick check $0.05 · Full audit $1.50 USDC.",
  "icon": "/favicon-32x32.jpg",
  "transport": "stdio",
  "command": "node",
  "args": ["dist/index.js"],
  "env": {
    "STRIPE_SECRET_KEY": { "required": true, "description": "Stripe secret key for payment verification" },
    "STRIPE_PAYMENT_URL": { "required": true, "description": "Stripe payment link for full audit ($1.50 USDC)" },
    "STRIPE_QUICK_CHECK_PAYMENT_URL": { "required": true, "description": "Stripe payment link for quick check ($0.05 USDC)" },
    "BRAVE_SEARCH_API_KEY": { "required": false, "description": "Brave Search API key" },
    "EXA_API_KEY": { "required": false, "description": "Exa neural search API key" },
    "USDC_WALLET_ADDRESS": { "required": false, "description": "USDC wallet address shown to payers" }
  },
  "tools": [
    {
      "name": "geo_quick_check",
      "description": "Quick brand visibility snapshot — score, top 3 citations, 2 quick tips. $0.05 USDC."
    },
    {
      "name": "audit_brand",
      "description": "Full brand visibility audit — score, 5 citations, LLM index status, 6 GEO recs. $1.50 USDC."
    },
    {
      "name": "get_payment_info",
      "description": "Get payment URLs and USDC wallet address for both audit tiers."
    }
  ]
}
