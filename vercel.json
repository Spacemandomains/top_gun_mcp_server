{
  "version": 2,
  "builds": [
    { "src": "public/**",   "use": "@vercel/static" },
    { "src": "api/**/*.ts", "use": "@vercel/node"   }
  ],
  "routes": [
    { "src": "/api/mcp(.*)", "dest": "/api/mcp/index.ts"       },
    { "src": "/api/(.*)",    "dest": "/api/$1"                  },
    { "src": "/.well-known/(.*)", "dest": "/public/.well-known/$1" },
    { "src": "/(.*)",        "dest": "/public/$1"               }
  ]
}
