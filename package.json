{
  "name": "top-gun-geo-lens",
  "version": "1.0.0",
  "description": "Top GUN — GEO-Lens Audit API with MCP wrapper. Brand visibility auditing across LLMs.",
  "type": "module",
  "scripts": {
    "dev":   "vercel dev",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "stripe":                    "^17.0.0",
    "zod":                       "^3.23.0"
  },
  "devDependencies": {
    "@vercel/node": "^5.0.0",
    "@types/node":  "^22.0.0",
    "typescript":   "^5.6.0"
  }
}
